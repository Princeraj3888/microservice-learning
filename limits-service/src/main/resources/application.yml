spring:
  application:
    name: limits-service

  config:
    import:
      - optional:configserver:http://localhost:8888
  profiles:
    active:
      - qa

limits-service:
  minimum: 21
  maximum: 991